<relations-index
  class="mt-8"
  :is-demo="<%= @is_demo %>"
  :banks="<%= @banks.to_json %>"
  :currencies="<%= SubAccount.currency.values %>"
  :all-investment-assets="<%= @investment_assets_all.to_json %>"
  :investment-instruments-investment-assets="<%= @investment_assets_instruments.to_json %>"
  :pension-funds-investment-assets="<%= @investment_assets_pension_funds.to_json %>"
  :real-estates-investment-assets="<%= @investment_assets_real_estates.to_json %>"
  :investment-assets-types="<%= @investment_assets_types.to_json %>"
  :account-types="<%= @account_types.to_json %>"
  :communes="<%= @communes.to_json %>"
  :real-estate-identifiers="<%= @real_estate_identifiers.to_json %>"
  :is-user="<%= @is_user %>"
  user-name="<%= "#{current_user.first_name} #{current_user.last_name}" %>"
  :document-type-options="<%= EnumUtils.enum_options_for_select(RelationFile, :document_type) %>"
  internal_id="<%= @internal_id %>"
>
<relations-index/>
